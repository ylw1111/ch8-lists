install.packages("styler")
install.packages("lintr")
styler:::style_active_file()
styler:::style_active_file()
install.packages("lintr")
styler:::style_pkg()
styler:::style_active_file()
install.packages("lintr")
install.packages("lintr")
styler:::style_pkg()
styler:::style_selection()
install.packages("lintr")
person <- list(name="Maggie", job="student")
person$name
person[[job]]
person$job
person[["job"]]
styler:::style_active_file()
styler:::style_active_file()
people <- list (p1=100, p2=200)
lapply(people, sqrt)
# Create a vector `my_breakfast` of everything you ate for breakfast
my_breakfast <- list(drink = "water", food = "banana")
# Create a vector `my_lunch` of everything you ate (or will eat) for lunch
my_lunch <- list(drink = "london fog tea latte", food = "meat ball spaghetti")
# Create a list `meals` that has contains your breakfast and lunch
meals <- list(my_breakfast, my_lunch)
# Create a list `meals` that has contains your breakfast and lunch
meals <- list("my_breakfast", "my_lunch")
meals
# Create a list `meals` that has contains your breakfast and lunch
meals <- list(my_breakfast, my_lunch)
meals
# Add a "dinner" element to your `meals` list that has what you plan to eat
# for dinner
meals$dinner <- "fremont baowl"
meals
# Use dollar notation to extract your `dinner` element from your list
# and save it in a vector called 'dinner'
dinner <- meals$dinner
dinner
# Use double-bracket notation to extract your `lunch` element from your list
# and save it in your list as the element at index 5 (no reason beyond practice)
meals[[my_lunch]]
# Use double-bracket notation to extract your `lunch` element from your list
# and save it in your list as the element at index 5 (no reason beyond practice)
meals[[my_lunch]]
meals[[my_lunch]]
# Use double-bracket notation to extract your `lunch` element from your list
# and save it in your list as the element at index 5 (no reason beyond practice)
meals[["my_lunch"]]
# Use double-bracket notation to extract your `lunch` element from your list
# and save it in your list as the element at index 5 (no reason beyond practice)
meals[[my_lunch]]
# Create a list `meals` that has contains your breakfast and lunch
meals <- list(c(my_breakfast, my_lunch))
meals
# Create a list `meals` that has contains your breakfast and lunch
meals <- c(my_breakfast, my_lunch)
meals
# Add a "dinner" element to your `meals` list that has what you plan to eat
# for dinner
meals$dinner <- "fremont baowl"
# Use dollar notation to extract your `dinner` element from your list
# and save it in a vector called 'dinner'
dinner <- meals$dinner
dinner
# Use double-bracket notation to extract your `lunch` element from your list
# and save it in your list as the element at index 5 (no reason beyond practice)
meals[[my_lunch]]
# Create a list `meals` that has contains your breakfast and lunch
meals <- c(list(my_breakfast, my_lunch))
meals
# Use double-bracket notation to extract your `lunch` element from your list
# and save it in your list as the element at index 5 (no reason beyond practice)
meals[[my_lunch]]
# Use double-bracket notation to extract your `lunch` element from your list
# and save it in your list as the element at index 5 (no reason beyond practice)
meals[["my_lunch"]]
# Use double-bracket notation to extract your `lunch` element from your list
# and save it in your list as the element at index 5 (no reason beyond practice)
meals[[my_lunch]]
# Create a list `meals` that has contains your breakfast and lunch
meals <- list(breakfast = my_breakfast, lunch = my_lunch)
# Use double-bracket notation to extract your `lunch` element from your list
# and save it in your list as the element at index 5 (no reason beyond practice)
meals[[lunch]]
lintr::lintr(4:4.R)
lintr::lintr(4:2.R)
lintr::lintr(exercise.R)
setwd("~/Desktop/INFO201B/in-class/ch8-lists/exercise-1")
lintr::lintr(exercise.R)
lintr::lint(exercise.R)
lintr::lint("exercise.R")
# Create a list `meals` that has contains your breakfast and lunch
meals <- list(breakfast = "my_breakfast", lunch = "my_lunch")
# Use double-bracket notation to extract your `lunch` element from your list
# and save it in your list as the element at index 5 (no reason beyond practice)
meals[[lunch]]
meals[["lunch"]]
# Create a list `meals` that has contains your breakfast and lunch
meals <- list(breakfast = my_breakfast, lunch = my_lunch)
# Use double-bracket notation to extract your `lunch` element from your list
# and save it in your list as the element at index 5 (no reason beyond practice)
meals[["lunch"]]
meals
# Use double-bracket notation to extract your `lunch` element from your list
# and save it in your list as the element at index 5 (no reason beyond practice)
meals[[5]] <- meals[["lunch"]]
# Use single-bracket notation to extract your breakfast and lunch from your list
# and save them to a list called `early_meals`
early_meals <- meals[1:2]
meals[[5]]
meals
# Create a list that has the number of items you ate for each meal
# Hint: use the `lappy()` function to apply the `length()` function to each item
items <- lapply(meals, length)
items
meal #return the new factor
meal
# Create a vector `better_meals` that is all your meals, but with pizza!
better_meals <- lapply(meals, add_food, "brocoli")
# Create a vector `better_meals` that is all your meals, but with pizza!
better_meals <- lapply(meals, add_pizza, "brocoli")
meal <- c(meals, "pizza")
meal
early_meals
